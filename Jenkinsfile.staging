pipeline {
    agent any
    
    stages {
        stage ("Build") {
            steps {
                script {
                    dockerapp = docker.build("api-youtube-staging", "-f ./dockerfile.staging .")
                }
            }
        }
    }
}